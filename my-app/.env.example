# Optional override. The app already uses this public API by default.
EXPO_PUBLIC_API_URL=https://cafeteria-api-3hqs.onrender.com
# For a local backend, replace with its reachable origin (without /api/v1).
